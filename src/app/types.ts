interface Server {
  id: string;
  name: string;
}

interface TeamType {
  id: number;
  label: string;
  value: string;
  maxMemberCount: number;
}

interface ClientType {
  id: number;
  label: string;
}
